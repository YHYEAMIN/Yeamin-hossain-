# 🆓 ফ্রি হোস্টিং + ফ্রি/সস্তা ডোমেইনে সাইট তোলা (ধাপে ধাপে)

এই প্রজেক্ট চালাতে ২টা জিনিস দরকার:

| # | জিনিস | কোথায় পাবেন (ফ্রি) |
|---|-------|---------------------|
| ১ | **ওয়েবসাইট হোস্ট** (কোড চলবে) | **Vercel** (সবচেয়ে সহজ) বা **Render** |
| ২ | **ডেটাবেজ PostgreSQL** (অর্ডার সেভ হবে) | **Neon** — ০.৫ GB, স্থায়ী ফ্রি |

> ⚠️ **Render-এর ফ্রি ডেটাবেজ ৩০ দিন পর মুছে যায়!** তাই ডেটাবেজ সবসময় **Neon** নিন, Render শুধু হোস্টিং হলে।

---

## ⚡ সবচেয়ে দ্রুত রাস্তা (Vercel + Neon) — ৪০ মিনিট, ৳০

### ধাপ ১ — কোড GitHub-এ তুলুন (৫ মিনিট)

1. https://github.com → **Sign up** (ফ্রি) → ইমেইল ভেরিফাই
2. উপরে ডানে **＋ → New repository** →
   - Repository name: `bitcamp-shop`
   - **Private** সিলেক্ট করুন
   - **Create repository**
3. আপনার কম্পিউটারে প্রজেক্ট ফোল্ডারে টার্মিনাল/CMD খুলুন (ফোল্ডারের ভেতরে রাইট-ক্লিক → "Open in Terminal"):

```bash
git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/আপনার-ইউজারনেম/bitcamp-shop.git
git push -u origin main
```

> GitHub-এ প্রথমবার push করলে লগইন উইন্ডো আসবে, সেখানে সাইন ইন করুন।

### ধাপ ২ — ফ্রি ডেটাবেজ নিন (Neon, ৫ মিনিট)

1. https://neon.tech → **Sign up** (GitHub দিয়ে, কার্ড লাগে না)
2. **Create project** →
   - Project name: `bitcamp`
   - Region: **Singapore** (বাংলাদেশ থেকে সবচেয়ে কাছের)
3. প্রজেক্ট ড্যাশবোর্ডে **Connection String** দেখতে পাবেন, কপি করুন। দেখতে এমন:

```
postgresql://abcd1234:xyz987@ep-cool-name-123.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
```

4. এই স্ট্রিংটা নোটপ্যাডে সেভ রাখুন ✅

### ধাপ ৩ — টেবিল তৈরি করুন (২ মিনিট, এটা বাদ দিলে অর্ডার সেভ হবে না)

আপনার কম্পিউটারে প্রজেক্ট ফোল্ডারে:

1. `.env` ফাইল খুলুন (নোটপ্যাডে)
2. শুধু এই লাইনটা বসান:

```
DATABASE_URL=postgresql://abcd1234:xyz987@ep-...neon.tech/neondb?sslmode=require
```

3. সেভ করে কমান্ড দিন:

```bash
npx drizzle-kit push
```

✅ `Changes applied` দেখলে টেবিল তৈরি — অর্ডার, কুরিয়ার, সেটিংস সব রেডি।

### ধাপ ৪ — Vercel-এ Deploy (১০ মিনিট)

1. https://vercel.com → **Sign Up** → **Continue with GitHub**
2. **Add New… → Project**
3. `bitcamp-shop` রিপো পাবেন → **Import** চাপুন
4. নিচে **Environment Variables** খুলে এগুলো একে একে যোগ করুন (**Add** চেপে):

| Name | Value (উদাহরণ) |
|------|----------------|
| `DATABASE_URL` | Neon-এর connection string |
| `ADMIN_PASSWORD` | `MyShop@2026` (আপনার পছন্দের) |
| `ADMIN_SECRET` | `x7f9k2m4p8q1z5r3t6u8v` (যেকোনো লম্বা র‍্যান্ডম) |
| `STEADFAST_API_KEY` | Steadfast থেকে পেলে |
| `STEADFAST_SECRET_KEY` | Steadfast থেকে পেলে |
| `STEADFAST_WEBHOOK_TOKEN` | যেকোনো র‍্যান্ডম (ওয়েবহুকের জন্য) |

5. **Deploy** চাপুন → ২-৩ মিনিট অপেক্ষা → 🎉 **লাইভ!**
6. লিংক পাবেন типа: **https://bitcamp-shop.vercel.app** (এটাই আপনার ফ্রি ডোমেইন)

### ধাপ ৫ — শেষ সেটআপ (৫ মিনিট)

- `https://bitcamp-shop.vercel.app/admin` → আপনার পাসওয়ার্ড দিয়ে লগইন
- **⚙️ সেটিংস** → আসল দাম/পণ্যের নাম/ডেলিভারি চার্জ বসান
- **🚚 কুরিয়ার** → Steadfast API Key বসিয়ে **সক্রিয়** করুন
- একটা টেস্ট অর্ডার দিয়ে কনফার্ম করে দেখুন কুরিয়ারে গেছে কিনা

> **পরবর্তীতে:** যেকোনো কোড বদলান → GitHub-এ push করলেই Vercel নিজে থেকে আপডেট করে নেবে (আর কিছু করতে হবে না)।

---

## ⚠️ Vercel ফ্রি প্ল্যানের শর্ত (জেনে রাখুন)

Vercel-এর **Hobby (ফ্রি)** প্ল্যান শুধু **ব্যক্তিগত/নন-কমার্শিয়াল** ব্যবহারের জন্য।
বিজনেস করে **প্রথম বিক্রি হওয়ার সাথে সাথে** দুটোর একটা করতে হবে:

| অপশন | খরচ | কবে |
|------|------|-----|
| **Vercel Pro** | ~$২০/মাস (২,৩০০৳) | সবচেয়ে সহজ, কিছু করতে হবে না |
| **Render-এ যাওয়া** | হোস্ট ৳০ (কিন্তু সাইট ১৫ মিনিট নিষ্ক্রিয় থাকলে ঘুমায়, ৩০-৬০ সেকেন্ড জাগ্রত হয়) | টাকা দেওয়ার আগে |

**আমার পরামর্শ:** শুরুতে Vercel ফ্রিতে ট্রাই করুন → প্রথম ১০-২০টা অর্ডার হয়ে গেলে Vercel Pro নিন (মাসে ২,৩০০৳, দোকানের খরচের তুলনায় খুব কম)।

---

## 🌐 ডোমেইন নিয়ে — কোনটা ফ্রি, কোনটা কেনা উচিত

### অপশন ১ — ফ্রি সাবডোমেইন (এখনই, ৳০)

Vercel-ই আপনার একটা ফ্রি লিংক দেয়:
```
https://bitcamp-shop.vercel.app
```
- HTTPS (সিকিউর) অটো ✅
- ভিজিটরদের জন্য যথেষ্ট ✅
- শুধু শেষে `vercel.app` লেখা থাকে

> **টিপ:** রিপোর নামটা সুন্দর দিন (যেমন `bitcamp-health` বা `arogya-supplement`) — লিংক সেই নামেই হবে।

### অপশন ২ — ফ্রি "আসল" ডোমেইন (ডেভেলপার ধরনের, ৳০)

| সাইট | দেখতে এমন | নেওয়ার নিয়ম |
|------|-----------|--------------|
| `is-a.dev` | `bitcamp.is-a.dev` | GitHub-এ রিপো বানিয়ে PR দিতে হয় (অ্যাপ্রুভ ১-৭ দিন) |
| `thedev.id` | `bitcamp.thedev.id` | একই নিয়ম, GitHub PR |
| `is-really.cool` | `bitcamp.is-really.cool` | GitHub PR |

⚠️ এগুলো **টেস্ট/প্রজেক্টের জন্য** ভালো, কিন্তু শেষে `.is-a.dev` থাকলে ক্রেতা বিজনেস হিসেবে কম ভরসা পায়।

### অপশন ৩ — সস্তা ডোমেইন (বিজনেসের জন্য সেরা, ~$১০-১২/বছশ ≈ ১,২০০-১,৪০০৳)

| রেজিস্ট্রার | .com দাম | রিনিউয়ে |
|-------------|----------|----------|
| **Cloudflare** (সবচেয়ে সস্তা) | ~$১০.৪৪ | $১০.৪৪ (সবসময় একই দাম) ✅ |
| Spaceship | ~$৯.৯৮ রিনিউ | $৯.৯৮ |
| Porkbun | ~$১০.৯৯ | $১০.৯৯ |
| Namecheap | কুপনে সস্তা | ~$১৪ |
| ❌ **GoDaddy** | ১ম বছর সস্তা দেখায় | **$২৩+** (আগুন!) |

**কেনা ডোমেইন Vercel-এ বসানো (৩ মিনিট):**
1. Vercel → আপনার প্রজেক্ট → **Settings → Domains**
2. আপনার ডোমেইন লিখুন (যেমন `bitcamp.com`) → **Add**
3. Vercel বলবে একটা **CNAME** রেকর্ড (যেমন `cname.vercel-dns.com`)
4. আপনার ডোমেইন কিনা সাইটে (Cloudflare/Hostinger) → **DNS** → CNAME যোগ করুন:
   - Type: `CNAME`, Name: `@`, Value: `cname.vercel-dns.com`
5. ৫-৩০ মিনিট পর HTTPS অটো চালু হয়ে `https://bitcamp.com` লাইভ 🎉

---

## 🔄 বিকল্প: Render-এ ফ্রি হোস্টিং (কমার্শিয়াল চালানো যায়)

Vercel-এর শর্ত না মানতে চাইলে Render-এ ফ্রিতে হোস্ট করুন:

1. https://render.com → GitHub দিয়ে সাইন আপ
2. **New + → Web Service** → `bitcamp-shop` রিপো কানেক্ট
3. সেটিংস:
   - **Build Command:** `npm install && npm run build`
   - **Start Command:** `npm start`
   - **Instance Type:** **Free**
4. Environment Variables: `DATABASE_URL` (Neon-এর), `ADMIN_PASSWORD`, `ADMIN_SECRET`
5. **Create Web Service** → লিংক: `https://bitcamp-shop.onrender.com`

⚠️ Render-এর সীমাবদ্ধতা:
- ১৫ মিনিট কেউ না আসলে সাইট ঘুমায়; প্রথম ক্লিকে **৩০-৬০ সেকেন্ড** লাগে (ভোক্তা চলে যাওয়ার ভয়)
- মাসে ৭৫০ ঘণ্টা ফ্রি (একটা সাইটের জন্য যথেষ্ট)
- **Render-এর নিজের ফ্রি ডেটাবেজ ৩০ দিনে মুছে যায়** — তাই Neon ব্যবহার করুন

---

## ✅ লাইভ করার আগের চেকলিস্ট

- [ ] GitHub-এ কোড push হয়েছে
- [ ] Neon-এ প্রজেক্ট তৈরি + connection string কপি
- [ ] `.env` তে `DATABASE_URL` বসিয়ে `npx drizzle-kit push` চালানো হয়েছে
- [ ] Vercel-এ ৪-৬টা Environment Variables দেওয়া হয়েছে
- [ ] Deploy সফল, লিংক খুলছে
- [ ] `/admin` এ লগইন হচ্ছে (নতুন পাসওয়ার্ডে)
- [ ] ⚙️ সেটিংসে আসল দাম/পণ্যের নাম বসানো হয়েছে
- [ ] 🚚 কুরিয়ারে Steadfast API Key বসানো ও সক্রিয়
- [ ] একটা টেস্ট অর্ডার → কনফার্ম → কুরিয়ারে গেছে কিনা দেখা হয়েছে
- [ ] ফোনের ব্রাউজারে লিংক খুলে দেখা হয়েছে
- [ ] বিজনেস শুরু হলে Vercel Pro বা Render-এ সরে নেওয়ার সিদ্ধান্ত নেওয়া হয়েছে

---

## 💰 মোট খরচ

| জিনিস | ফ্রিতে শুরু | বিজনেস চলার পর |
|-------|-------------|----------------|
| হোস্ট | ৳০ | Vercel Pro ~২,৩০০৳/মাস |
| ডেটাবেজ | ৳০ (Neon ০.৫ GB) | অনেক অর্ডার হলে Neon Pro ~$১৯/মাস |
| ডোমেইন | ৳০ (`.vercel.app`) | ~১,৪০০৳/বছশ |
| SSL | ৳০ | ৳০ |
| **মোট** | **৳০** | মাসে ~২,৫০০-৩,০০০৳ |
